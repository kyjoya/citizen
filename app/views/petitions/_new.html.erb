<%= form_for [@state, @petition || Petition.new] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.label :image %>
  <%= f.file_field :image %>

  <%= f.submit "Submit", class: "small button" %>
<% end %>
